<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">

            <!--设置沉浸式状态栏背景颜色-->
            <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
            <!--设置各控制元件(如：EditText光标、check box、switch 或是 radoi) 被勾选 (checked) 或是选定 (selected) 的颜色-->
            <item name="colorAccent">@color/colorAccent</item>
            <!-- toolBar背景颜色-->
            <item name="colorPrimary">@color/colorPrimary</item>
            <!-- 标题字体颜色-->
            <item name="android:textColorPrimary">@color/textColorPrimary</item>
            <!-- toolBar高度-->
            <item name="actionBarSize">@dimen/title_height</item>
            <!-- 设置输入框不被软键盘遮挡-->
            <item name="android:windowSoftInputMode">adjustPan</item>
            <!-- 分割线-->
    </style>

    <style name="AppTheme.Launcher" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>

    </style>
    <!-- 提示dialog-->
    <style name="alert_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>




    <!-- **************************自定义仿IOS的AlertDialog的样式************************** -->
    <!-- 自定义仿IOS的ActionSheet底部Dialog的样式 ，有模糊效果 -->
    <style name="ActionSheetDialogStyle" parent="@android:style/Theme.Dialog">

        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/ActionSheetDialogAnimation</item>
    </style>
    <!-- ActionSheet进出动画 -->
    <style name="ActionSheetDialogAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/actionsheet_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/actionsheet_dialog_out</item>
    </style>

    <style name="ActivityDialogStyle" parent="@style/Theme.AppCompat.Dialog">
        <!--是否半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--是否显示标题-->
        <item name="android:windowNoTitle">true</item>　
        <!--设置dialog的背景-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--背景是否模糊显示-->
        <item name="android:backgroundDimEnabled">true</item>
        <!--设置窗口内容不覆盖-->
        <item name="android:windowContentOverlay">@null</item>
        <!--点击空白地方关闭-->
        <item name="android:windowCloseOnTouchOutside">false</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--设置动画，在这里使用让它继承系统的Animation.Dialog-->
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>
    <style name="FlowLayout">

        //选项item是否可以点击
        <item name="item_click">true</item>

        //选项item文字默认值
        <item name="item_textColor">#3E4248</item>

        //选项item背景色
        <item name="item_backgroundColor">#ffffff</item>

        //选中后选项item的文字颜色
        <item name="item_selectedTextColor">#438BE8</item>

        //选中后选项item的背景颜色
        <item name="item_selectedBackgroundColor">#DBEAFF</item>

        //选项item的圆角大小
        <item name="item_radius">1dp</item>

        //选项item的边缘颜色
        <item name="item_strokeColor">#ffb7b7b7</item>

        //选项item选中后边缘颜色
        <item name="item_selectedStrokeColor">#ff438be8</item>

        //选项item边缘的大小
        <item name="item_strokeWidth">2px</item>

        //水平方向，选项item的间距
        <item name="item_horizontalSpacing">10dp</item>

        //选项item内部padding
        <item name="item_horizontalPadding">8dp</item>

        ////竖直方向，选项item的间距
        <item name="item_verticalPadding">10dp</item>


        //选项item的大小，如果设置wrap_content则包裹它的内容，可设置固定值如80，单位dp
        <item name="item_width">wrap_content</item>
        <item name="item_height">wrap_content</item>


        //限制item文字显示长度
        <item name="item_maxEms">50</item>
        <item name="item_singleLine">true</item>

        //限制item文字大小
        <item name="item_textSize">13dp</item>

        //是否可以多选
        <item name="item_multiChooseable">true</item>
    </style>
    <declare-styleable name="CircleImageView">
        <attr name="border_width" format="dimension" />
        <attr name="border_color" format="color" />
    </declare-styleable>
</resources>